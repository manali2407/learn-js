
// datatype
// number => 2 to power 53
// BigInt
// string => ""
// Boolean => true/false
// null =>standalone value
// undefined =>
// Symbol=> unique

let age = 25
let firstName="manali"
let logIn = true
let city = null
let state = undefined

console.table([typeof(age), typeof(firstName), typeof(logIn),typeof(city),typeof(state)]);

console.table(typeof[(age),(firstName),(logIn),(city),(state)]);